name: Build

on:
  create:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: ./.github/actions/context
    - name: Build
      id: build
      uses: ./.github/actions/build

